{
  "tasks": {
    "start": "deno run --allow-ffi --allow-sys --allow-net --allow-env --allow-read --allow-write mod.ts"
  },
  "compilerOptions": {
    "strict": true
  },
  "lint": { "rules": { "exclude": ["no-explicit-any"] } }
}
