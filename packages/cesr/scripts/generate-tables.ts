// Placeholder generator entrypoint. In the next step this should ingest pinned
// upstream snapshots and emit the *.generated.ts table artifacts.
console.log("Tables are currently committed in generated TS files.");
