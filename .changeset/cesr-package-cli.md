---
"cesr-ts": minor
---

BREAKING CLI CHANGE: replace the `cesr-annotate` npm executable with a
package-level `cesr` CLI. The annotation workflow is now `cesr annotate`,
validation is available as `cesr validate`, and parser benchmarking is
available as `cesr bench`.
