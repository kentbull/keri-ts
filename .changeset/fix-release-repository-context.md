---

---

Give GitHub CLI explicit repository context when creating releases from a job without a source checkout.

Select expensive PR checks from the changed paths so documentation and unrelated workflow changes do not consume full unit, interop, and package runs.
