---
"keri-ts": patch
"@keri-ts/tufa": patch
---

Add a Sally-like verifier agent with durable verifier cue storage, grant-driven
presentation processing, revocation webhook handling, schema-validator config
support, and the `tufa verifier run` command.
